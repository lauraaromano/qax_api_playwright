info:
  name: verifica status
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3333/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
